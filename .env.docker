# Stremio BitTorrent API - Docker Environment Configuration
# Copy this file to .env.docker and customize for your deployment

# Optional: TMDB API Key for metadata (leave empty if not needed)
TMDB_API_KEY=

# You can override any other environment variables here
# All other variables have sensible defaults in docker-compose.yml
